import resources from "./resources";
// import tags, { languages, types, PRICES, CATEGORIES  } from "./tags";

export {
  resources,
  // tags,
  // languages,
  // types,
  // PRICES,
  // CATEGORIES,
}